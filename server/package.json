{
  "name": "notepad-calculator-server",
  "version": "1.0.0",
  "description": "Local fallback service: sends lines the local rule engine can't parse to Qwen (DashScope) and returns a numeric answer.",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
